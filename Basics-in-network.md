# أساسيات Host Bits و Network و Broadcast في Subnetting

## تقسيم عنوان IPv4

عنوان IPv4 يتكون من **32 Bit** وينقسم إلى:

```
[ Network Bits ] [ Host Bits ]
```

* **Network Bits:**

  * تحدد الشبكة التي ينتمي إليها العنوان.
  * لا تُستخدم لتعريف الأجهزة.

* **Host Bits:**

  * تحدد رقم الجهاز داخل الشبكة.
  * كل جهاز يحتاج قيمة مختلفة داخل جزء الـ Host.

---

# لماذا نحتاج Host Bits؟

لأن الأجهزة داخل الشبكة  تحتاج عناوين مختلفة.

مثال:

```
192.168.1.1
192.168.1.2
192.168.1.3
```

الجزء الأخير يمثل رقم الجهاز (Host).

إذا لم يبقَ أي Bit للـ Host مثل:

```
/32
```

فهذا يعني أن كل الـ 32 Bit للشبكة، ولن يوجد مكان لتعريف أجهزة متعددة.

---

# Network Address

* أول عنوان في أي Subnet يسمى **Network Address**.
* وظيفته تعريف الشبكة نفسها.
* لا يُعطى لأي جهاز.

مثال:

```
192.168.1.0/24
```

العنوان:

```
192.168.1.0
```

يمثل اسم الشبكة.

---

# Broadcast Address

* آخر عنوان في أي Subnet يسمى **Broadcast Address**.
* يستخدم لإرسال رسالة إلى جميع الأجهزة داخل نفس الشبكة.
* لا يُعطى لأي جهاز.

مثال:

```
192.168.1.255
```

---

# لماذا نحجز Network و Broadcast؟

لأن الشبكة تحتاج:

1. عنوان يحدد الشبكة نفسها.
2. عنوان لإرسال رسالة لكل الأجهزة.

لذلك:

* أول عنوان = Network.
* آخر عنوان = Broadcast.
* العناوين بينهما = Hosts.

---

# معنى 256 و 255

كل جزء من عنوان IPv4 حجمه **8 Bits**.

عدد الاحتمالات:

```
2⁸ = 256
```

إذن يوجد 256 قيمة.

لكن القيم تبدأ من الصفر:

```
0 → 255
```

لذلك:

* **256** = عدد العناوين الممكنة.
* **255** = أكبر رقم يمكن كتابته في Octet.

---

# مثال /24

```
192.168.1.0/24
```

لدينا:

```
32 - 24 = 8 Host Bits
```

عدد العناوين:

```
2⁸ = 256
```

النطاق:

```
192.168.1.0
إلى
192.168.1.255
```

الاستخدام:

| العنوان                     | الاستخدام |
| --------------------------- | --------- |
| 192.168.1.0                 | Network   |
| 192.168.1.1 - 192.168.1.254 | Hosts     |
| 192.168.1.255               | Broadcast |

عدد الأجهزة:

```
256 - 2 = 254 Hosts
```

---

# لماذا أقل Subnet تقليديًا هي /30؟

في CCNA يتم استخدام:

```
/30
```

لأنه يترك:

```
32 - 30 = 2 Host Bits
```

عدد العناوين:

```
2² = 4
```

مثال:

```
192.168.1.0/30
```

| العنوان     | الاستخدام |
| ----------- | --------- |
| 192.168.1.0 | Network   |
| 192.168.1.1 | Host      |
| 192.168.1.2 | Host      |
| 192.168.1.3 | Broadcast |

إذن يوجد:

```
2 Hosts فقط
```

---

# لماذا ليس /31؟

في:

```
/31
```

يبقى Bit واحد للـ Host:

```
2¹ = 2 عناوين
```

مثال:

```
192.168.1.0/31
```

العناوين:

```
192.168.1.0
192.168.1.1
```

حسب القاعدة التقليدية:

* الأول Network.
* الثاني Broadcast.

فلن يبقى أي Host.

لكن يستخدم /31 في حالة خاصة:

**Point-to-Point بين راوترين**

مثال:

```
Router A -------- Router B

192.168.1.0/31
```

* Router A يأخذ عنوان.
* Router B يأخذ عنوان.
* لا يوجد Broadcast مطلوب.

---

# القواعد المهمة للحفظ في CCNA

* IPv4 = 32 Bit.
* Host Bits تستخدم لإعطاء عناوين للأجهزة.
* Network Address = أول عنوان.
* Broadcast Address = آخر عنوان.
* عدد العناوين = `2^Host Bits`.
* عدد الأجهزة المتاحة = `2^Host Bits - 2`.
* `/24`:

  * 256 عنوان.
  * 254 جهاز.
* `/30`:

  * 4 عناوين.
  * 2 أجهزة.
* `/31`:

  * يستخدم غالبًا بين راوترين فقط.
* 256 = عدد الاحتمالات.
* 255 = أكبر قيمة يمكن كتابتها في Octet.
