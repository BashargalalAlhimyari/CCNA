8






تتناول المصادر شرحاً مفصلاً لعملية **تغليف البيانات (Encapsulation)** عبر طبقات نموذج OSI السبع، وكيفية تغير مسمياتها ومكوناتها في كل طبقة حتى تصل إلى الوسط الفيزيائي، وإليك الشرح الكامل لكل النقاط الواردة:

### 1. عملية تغليف البيانات (Encapsulation) وتجزئتها
*   في كل طبقة من طبقات النموذج السبع، يضاف غلاف للبيانات [1].
*   في الطبقات الثلاث العليا (السابعة، والسادسة، والخامسة)، تظل البيانات محتفظة بشكلها كملف كامل وتسمى **"داتا" (Data)** لأنها لم تتقطع بعد [1].
*   تبدأ عملية **تقطيع الملف (Fragmentation)** إلى قطع صغيرة في الطبقة الرابعة (**Transport Layer**)، وهنا يبدأ تغير اسم البيانات [1].

### 2. مسميات البيانات في كل طبقة (PDU)
يُطلق على اسم البيانات في كل طبقة مصطلح **PDU** (اختصار لـ Protocol Data Unit)، وهي كالتالي:
*   **الطبقة 7، 6، 5:** تسمى **داتا (Data)** [3، 4].
*   **الطبقة 4 (Transport):** تسمى **سيجمنت (Segment)**، وهي عبارة عن الداتا المقطوعة مضافاً إليها هيدر الطبقة الرابعة [1، 4].
*   **الطبقة 3 (Network):** تسمى **باكيت (Packet)**، وهي عبارة عن "السيجمنت" مضافاً إليها هيدر الطبقة الثالثة [2، 4].
*   **الطبقة 2 (Data Link):** تسمى **فريم (Frame)**، وهي عبارة عن "الباكيت" مضافاً إليها هيدر في البداية وتذييل (Trailer) في النهاية [3، 4، 5].
*   **الطبقة 1 (Physical):** تسمى **بتس (Bits)**، حيث تتحول البيانات إلى واحدات وأصفار [3، 4].

### 3. تفاصيل هيدر الطبقات (Headers)
يختلف محتوى الهيدر (الترويسة) بناءً على الطبقة والبروتوكول المستخدم:
*   **هيدر الطبقة الرابعة:** يعتمد على البروتوكول؛ ففي **TCP** يحتوي على رقم منفذ المصدر والهدف، وحجم النافذة (Window size)، والرقم التسلسلي (Sequence). أما في **UDP** فيحتوي فقط على المنافذ والـ Checksum [2].
*   **هيدر الطبقة الثالثة (IP Header):** يحتوي على عنوان IP المرسل (Source) والمستقبل (Destination)، ومدة بقاء الحزمة (TTL)، والإصدار (Version)، والـ Checksum [2].
*   **الطبقة الثانية (الفريدة):** هي الطبقة الوحيدة التي تضيف **هيدر وتذييل (Trailer)** [2، 6]. الهيدر يحتوي على عناوين الماك (MAC addresses)، بينما التذييل يُستخدم للتحقق من الأخطاء [3].

### 4. هيكلة "الفريم" (Frame Structure) في الطبقة الثانية
يتكون الفريم (مثل فريم الإيثرنت) من عدة أجزاء:
*   **الـ Preamble (الديباجة):** هي بايتات ثابتة (أصفار وواحدات) تشير إلى بداية الفريم لتنبيه المستقبل [4].
*   **عناوين الماك (MAC Addresses):** عنوان الماك للمستقبل أولاً، ثم عنوان الماك للمرسل [6، 8].
*   **النوع (Type):** يحدد نوع البيانات المحمولة [5].
*   **البيانات (Packet/Data):** وهي الباكيت القادمة من الطبقة الأعلى [5].
*   **FCS:** للتحقق من سلامة البيانات في التذييل [5].

### 5. العناوين الفيزيائية (MAC) مقابل العناوين المنطقية (IP)
هناك قاعدة مهمة جداً فيما يخص حركة البيانات عبر الشبكة:
*   **العنوان المنطقي (IP):** يظل **ثابتاً** من بداية الطريق حتى نهايته (من المصدر إلى الهدف النهائي) ولا يتغير [10، 11].
*   **العنوان الفيزيائي (MAC):** هو الذي **يتغير في كل نقطة (Router)** يمر بها القلم (البيانات) [8، 11]. ففي كل راوتر، يتم فك التغليف وتغيير عنوان الماك ليكون المصدر هو الراوتر الحالي والوجهة هي الراوتر التالي، بينما يظل الـ IP ثابتاً [10، 11].

باختصار، عملية التغليف تضمن إضافة المعلومات اللازمة لكل طبقة لضمان وصول البيانات بشكل صحيح، مع تمييز دقيق بين العناوين التي توجه البيانات محلياً (MAC) وتلك التي توجهها عبر الشبكات الواسعة (IP) [6، 11].

نعم، الشرح السابق غطى المحاور الأساسية، ولكن التسجيل الصوتي يحتوي على **تفاصيل دقيقة، أمثلة توضيحية، وقصص جانبية** لم تُذكر بالكامل في الملخص السريع. إليك النقاط الإضافية والتفصيلية التي وردت في التسجيل:

### 1. تفاصيل دقيقة عن "الديباجة" (Preamble)
*   **تكوينها التقني:** تتكون من **8 بايتات** (أو 64 بت) من الأصفار والواحدات المتناوبة (10101010)، لكن البايت الأخير ينتهي بـ **(11)** للإشارة إلى نهاية الديباجة وبداية الفريم [14، 16].
*   **تطور وظيفتها:** في كروت الشبكة القديمة (سرعة 10)، كانت تُستخدم **للتزامن (Synchronization)** ليعرف المستقبل متى يبدأ الفريم [14، 15]. أما في السرعات الحديثة (100 و1000 وما فوق)، أصبح التزامن يتم عبر دوائر هاردوير خاصة، وبقيت الديباجة فقط من أجل **التوافق (Compatibility)** مع الأجهزة القديمة [15، 16].

### 2. وظيفة حقل "النوع" (Type) في الفريم
*   هذا الحقل ضروري جداً عند عملية **فك التغليف (Decapsulation)**؛ فهو الذي يخبر الطبقة الثانية أي بروتوكول في الطبقة الثالثة يجب أن يستلم "الباكيت" (هل هو IPv4 أم IPv6 أم غيرهما) [17، 18].

### 3. آلية فحص الأخطاء (FCS/CRC) بالتفصيل
*   **خوارزمية CRC:** يقوم المرسل بتشغيل خوارزمية رياضية على بيانات الهيدر والباكيت لتوليد قيمة رقمية تُخزن في حقل **FCS** [23، 24].
*   **المطابقة عند المستقبل:** عند وصول الفريم، يعيد المستقبل حساب القيمة بنفس الخوارزمية؛ إذا تطابقت القيمتان يكون الفريم سليماً، وإذا اختلفتا (حتى لو في بت واحد فقط) يتم **حذف الفريم فوراً** [24، 25].
*   **اكتشاف الخطأ مقابل تصحيحه:** الطبقة الثانية تكتشف الخطأ وتحذف الفريم فقط، لكنها **لا تعيد إرساله** [1]. المسؤول عن طلب إعادة الإرسال وتصحيح الخطأ هو بروتوكول **TCP** في الطبقة الرابعة، أو قد يُترك الأمر للتطبيق في الطبقة السابعة إذا كان البروتوكول المستخدم هو **UDP** [2].

### 4. أمثلة وقصص توضيحية من المحاضر
*   **قصة الـ 50 ألف ريال:** ضرب المحاضر مثالاً بأب يرسل لابنه 50 ألف ريال مع "صاحب صالون" ويرسل معه رسالة ورقية توضح المبلغ؛ فإذا وجد الابن المبلغ ناقصاً عن المكتوب في الرسالة، سيعرف أن هناك خطأ (وهذا يشبه دور الـ FCS في كشف التلاعب بالبيانات) [19، 21، 22].
*   **الخلفية التاريخية للتقنية:** ذكر المحاضر ذكرياته عن دراسة الكمبيوتر في نهاية التسعينات (تخرج عام 99)، وتحدث عن مواصفات الأجهزة القديمة مثل معالجات Pentium 1 ورامات بسعة ضئيلة جداً، وكيف كانت سرعات الشبكة بطيئة وتحتاج "للتزامن" اليدوي [20، 21].
*   **التطبيقات العملية للهاش (Hash):** أشار إلى أن فكرة الـ CRC تشبه الـ Hash المستخدم عند تحميل أنظمة التشغيل (مثل IOS للراوترات) للتأكد من أن الملف سليم ولم يتم التلاعب به من قبل مخترقين [27، 28].

بناءً على المصادر، هذه هي التفاصيل الكاملة والدقيقة التي تضمنها التسجيل الصوتي بالإضافة إلى المعلومات التقنية الأساسية التي ذُكرت سابقاً. [1-28]



بناءً على التسجيل الصوتي الذي قدمته، إليك شرح مفصل وشامل لكل النقاط التي وردت فيه، مرتبة حسب المواضيع التقنية والتوضيحية:

### 1. الديباجة (Preamble) وتطور وظيفتها
*   **تكوينها التقني:** تتكون الديباجة من **8 بايتات** (أي 64 بت). السبعة بايتات الأولى عبارة عن واحدات وأصفار متناوبة (10101010)، أما البايت الثامن فينتهي بـ **(11)**؛ وهذا التشكيل هو الذي ينبه المستقبل بأن الفريم سيبدأ مباشرة بعد هذه الإشارة [1].
*   **وظيفة التزامن (Synchronization):** في شبكات الإيثرنت القديمة (سرعة 10)، كان التزامن "سوبر وير"؛ أي أن المستقبل يحتاج للديباجة ليعرف أين يبدأ الفريم ليقوم بتخزينه في "بفر" (Buffer) خاص به [1، 2].
*   **وظيفة التوافق (Compatibility):** في السرعات الحديثة (100، 1000، وما فوق)، أصبح التزامن يتم عبر دوائر "هاردوير" خاصة ولا حاجة تقنية للديباجة، ولكن تم الإبقاء عليها لضمان **التوافق** مع الكروت القديمة؛ فلو حُذفت لن تستطيع الأجهزة الحديثة التواصل مع القديمة [2، 3].

### 2. حقل النوع (Type) وعملية فك التغليف
*   عند وصول البيانات للمستقبل، تبدأ عملية **فك التغليف (Decapsulation)**؛ حيث تُحذف ترويسة وتذييل الطبقة الثانية لتصعد البيانات للطبقة الثالثة (النتورك) [2].
*   **وظيفة حقل النوع:** هو المسؤول عن تحديد أي بروتوكول في الطبقة الثالثة سيستلم هذه البيانات (مثل IPv4، أو IPv6، أو غيرهما) [4، 5]. بدون هذا الحقل، لن تعرف الطبقة الثانية لمن تُسلم "الباكيت" الصاعدة [3].

### 3. آلية فحص الأخطاء (FCS / CRC)
*   **التعريف:** الـ FCS (اختصار لـ Frame Check Sequence) هو التذييل الذي يضاف في نهاية الفريم ويستخدم خوارزمية **CRC** لاكتشاف الأخطاء [6، 10].
*   **طريقة العمل:**
    *   **عند المرسل:** يقوم كرت الشبكة بتشغيل خوارزمية CRC على الهيدر والباكيت لتوليد قيمة رقمية تُوضع في حقل FCS [10، 11].
    *   **عند المستقبل:** يعيد المستقبل حساب القيمة لنفس البيانات؛ إذا **تطابقت** القيمة مع ما هو موجود في FCS، فالفريم سليم [11، 12].
    *   **في حال الخطأ:** إذا اختلف الرقم (حتى لو بسبب تغير بت واحد فقط من صفر لواحد)، يقوم المستقبل **بحذف الفريم فوراً** [10، 12].
*   **كشف الخطأ لا يعني إصلاحه:** الطبقة الثانية تكتشف الخطأ وتحذف الفريم فقط [4]. أما **تصحيح الخطأ** وإعادة الإرسال فهي وظيفة الطبقة الرابعة (بروتوكول TCP) أو قد تترك للتطبيق في الطبقة السابعة إذا كان البروتوكول المستخدم هو UDP [9، 13].

### 4. أمثلة توضيحية وقصص جانبية
*   **مثال الـ 50 ألف ريال:** شبّه المحاضر الـ FCS برسالة يرسلها أب مع مبلغ مالي؛ إذا وجد الابن في الرسالة مبلغاً يختلف عما استلمه فعلياً، سيعرف بوجود خطأ وتلاعب (وهذا هو دور الـ FCS في كشف سلامة البيانات) [6، 8، 9].
*   **مبدأ الهاش (Hash):** فكرة الـ CRC تشبه الـ Hash المستخدم في تحميل البرامج والأنظمة؛ حيث توفر الشركات (مثل سيسكو) رمز هاش للملف للتأكد من أنه سليم ولم يتم التلاعب به من قبل مهكرين أثناء التحميل [14، 15].
*   **تطور الحاسوب:** ذكر المحاضر ذكرياته التقنية حيث تخرج عام 1999، وكانت الأجهزة حينها (Pentium 1) باهظة الثمن (1200 دولار) وبمواصفات بسيطة جداً (رام ضئيلة وهاردسك 1 جيجا)، ولم يكن هناك يوتيوب أو تطبيقات ثقيلة [7، 8].

باختصار، يركز هذا الجزء من التسجيل على كيفية ضمان **التزامن** بين الأجهزة، وكيفية **توجيه** البيانات للبروتوكول الصحيح، والأهم من ذلك كيفية **التحقق من سلامة البيانات** وحذفها في حال حدوث أي خطأ أثناء النقل الفيزيائي [3، 5، 12].


بناءً على المصادر، إليك الإجابة المفصلة على تساؤلاتك حول تقنيات إطار الإيثرنت:

### لماذا يتم التخلص من الإطارات التي يقل حجمها عن 64 بايت؟
يتم التخلص من هذه الإطارات لأن نظام الإيثرنت لديه **قاعدة محددة لحجم الإطار**؛ حيث أن **أصغر حجم مسموح به هو 64 بايت** وأكبر حجم هو 1518 بايت [1]. إذا وصل إطار بحجم أقل من 64 بايت أو أكبر من 1518 بايت، فإنه يُعتبر **"إطاراً خاطئاً" (Error Frame)** يحتوي على خلل ما، وبالتالي يتم حذفه ويُطلب من المرسل إعادة إرساله [1].

### أين تضاف عملية الحشو (Padding) ولماذا؟
*   **أين تضاف؟** تضاف عملية الحشو **داخل منطقة البيانات (Data)** فقط، ولا يمكن إضافتها في "الهيدر" (Header) لأن ذلك سيؤدي إلى تغيير قيم العناوين المسجلة فيه [2].
*   **لماذا تضاف؟** تُستخدم عندما تكون البيانات في الإطار الأخير غير كافية للوصول إلى الحد الأدنى للحجم المطلوب [4، 5]. والهدف منها هو **حماية البيانات وضمان ثبات الإطار**؛ حيث يشبّه المصدر ذلك بوضع "قش" أو مواد حشو داخل كرتون يحتوي على زجاج؛ فإذا لم يكن الكرتون ممتلئاً، سيتحرك الزجاج وينكسر، لذا يعمل الحشو على تثبيت البيانات ومنع تضررها أثناء النقل [4، 5].

### ما هو مصطلح الـ MTU؟
مصطلح **MTU** هو اختصار لـ **(Maximum Transfer Unit)** أو وحدة النقل القصوى [3]. وهو يشير إلى **أقصى حجم للباكيت (Packet)** يمكن إرساله عبر الشبكة، وعادة ما يكون **1500 بايت** في شبكات الإيثرنت [3، 4]. تبرز أهميته لأن الملفات الكبيرة (مثل ملف بحجم 5 أو 10 ميجا) لا يمكن إرسالها دفعة واحدة، بل يجب تقطيعها إلى أجزاء صغيرة بحجم الـ MTU لتسهيل نقلها عبر الشبكة وإعادة تجميعها عند الطرف الآخر [3، 4].


بناءً على المصادر المقدمة، إليك شرح مفصل لمكونات ترويسة (Header) بروتوكولات النقل الأساسية **TCP** و **UDP**، والفرق بينهما، مع توضيح لكيفية عمل بعض الخصائص التقنية:

### أولاً: ترويسة بروتوكول TCP (Transmission Control Protocol)
تعد ترويسة TCP معقدة لأنها مصممة لضمان موثوقية نقل البيانات، ويبلغ طولها الأدنى **20 بايت** (بدون الخيارات الإضافية) [1]. وتتكون من الحقول التالية:

*   **منفذ المصدر والوجهة (Source & Destination Ports):** يخصص لكل منهما **16 بت**. عادة ما يكون رقم منفذ المصدر عشوائياً وأكبر من 1023، بينما يحدد منفذ الوجهة نوع الخدمة (مثلاً المنفذ 23 مخصص لخدمة Telnet) [2, 3].
*   **الرقم التسلسلي (Sequence Number):** طوله **32 بت**، ويستخدم لترتيب الرزم وضمان وصولها بالتسلسل الصحيح [2].
*   **رقم التأكيد (Acknowledgment Number):** طوله **32 بت**، وهو يمثل **الرقم المتوقع التالي** للرزمة التي وصلت بنجاح. فإذا استلم المستقبل 10000 رزمة، فإنه يرسل رقم التأكيد 10001 ليخبر المرسل أنه ينتظر الرزمة التالية [1, 4].
*   **طول الترويسة (Header Length) والحقل المحجوز (Reserved):** يخصص **4 بت** لطول الترويسة، و **4 بت** أخرى كحقل محجوز للاستخدامات المستقبلية [2].
*   **الأعلام (Flags):** هي مؤشرات تحدد حالة الرزمة، مثل:
    *   **SYN:** لبدء الاتصال (Synchronization).
    *   **ACK:** للتأكيد على استلام البيانات.
    *   **FIN:** لإنهاء الاتصال.
    *   **RST:** لإعادة ضبط الاتصال في حال حدوث خطأ [2, 3].
*   **حجم النافذة (Window Size):** يخصص له **16 بت**، ووظيفته **تنسيق سرعة الإرسال** بين المرسل والمستقبل (Flow Control) لضمان عدم إرسال بيانات أسرع مما يمكن للمستقبل معالجته [2].
*   **التحقق من الخطأ (Checksum) والمؤشر العاجل (Urgent Pointer):** يخصص لكل منهما **16 بت** [1].

### ثانياً: ترويسة بروتوكول UDP (User Datagram Protocol)
يتميز UDP بكونه بروتوكولاً بسيطاً وسريعاً جداً لأن ترويسته صغيرة جداً مقارنة بـ TCP، حيث يبلغ طولها **8 بايت** فقط [5]. وتتكون من:
*   **منفذ المصدر (16 بت)**.
*   **منفذ الوجهة (16 بت)**.
*   **الطول (16 بت)**.
*   **حقل التحقق (Checksum) (16 بت)** [5].
*   **ملاحظة:** لا يحتوي UDP على ميزات مثل حجم النافذة، الأعلام، الأرقام التسلسلية، أو خيارات الأمان، مما يجعله أسرع لكنه أقل موثوقية [5].

### ثالثاً: تقسيم أرقام المنافذ (Port Numbers)
تنقسم أرقام المنافذ إلى ثلاثة أنواع رئيسية:
1.  **المنافذ المعروفة (Well-known Ports):** من **0 إلى 1023**، وهي محجوزة لخدمات محددة [6].
2.  **المنافذ المسجلة (Registered Ports):** من **1024 إلى 49151** [6].
3.  **المنافذ الديناميكية أو الخاصة (Dynamic/Private):** من **49152 إلى 65535**، وهي مجانية للاستخدام داخل الشبكات المحلية [6].

### رابعاً: أحجام البيانات (Data Sizes)
*   أقصى حجم للرزمة (Packet) في الطبقة الثالثة هو **1500 بايت** [7].
*   في الطبقة الثانية، يصل حجم الإطار (Frame) إلى **1518 بايت** (بإضافة 18 بايت لترويسة الطبقة الثانية) [7].
*   عند استخدام TCP، يتم خصم 20 بايت للترويسة، مما يترك حوالي **1480 بايت** للبيانات الفعلية (Segment) [7].
].


يتناول المحتوى الذي قدمته شرحاً مفصلاً لآلية **تقطيع البيانات (Segmentation)** وهيكلة **إطار الإيثرنت (Ethernet Frame)** وكيفية التعامل مع أحجام البيانات المختلفة عبر طبقات الشبكة، وتحديداً الطبقتين الثانية والرابعة.

إليك شرح كامل للنقاط التي وردت في المصادر:

### 1. مسؤولية الطبقة الرابعة (TCP)
تعتبر الطبقة الرابعة مسؤولة عن **تقطيع البيانات** الكبيرة وإضافة "هيدر" (Header) خاص بها [1]. هذا الهيدر يحتوي على حقول أساسية تشمل:
*   **منفذ المصدر (Source Port):** حيث أن الطبقة الرابعة هي المسؤولة عن المنافذ [1, 2].
*   **منفذ الوجهة (Destination Port)** [2].

### 2. هيكلة وعنونة إطار الإيثرنت
تتضمن الطبقة الثانية (Link Layer) تفاصيل محددة لحجم العناوين والبيانات:
*   **عنوان MAC:** يبلغ طوله **48 بت**، وهو ما يعادل **6 بايت** [2].
*   **حقل FCS (Frame Check Sequence):** وهو حقل مخصص لفحص الأخطاء وطوله **4 بايت** [2].
*   **حجم الحزمة (Packet):** الحزم القادمة من الطبقة الثالثة يتراوح حجمها من **46 إلى 1500 بايت** [2].

### 3. وحدة النقل القصوى (MTU) وعملية التقطيع
نظراً لأن الملفات قد تكون كبيرة جداً (مثل 5 أو 10 ميجابايت)، لا يمكن إرسالها دفعة واحدة عبر الشبكة [3]. لذلك:
*   يتم تقطيع الملفات إلى أجزاء صغيرة تسمى **MTU (Maximum Transfer Unit)** [3].
*   يبلغ الحجم المعتاد لـ MTU في شبكات الإيثرنت **1500 بايت** [3, 4].
*   عندما تنزل البيانات للطبقة الرابعة، يتم تقسيمها بحيث لا يتجاوز حجم القطعة الواحدة مع الهيدر هذا الحد [4].

### 4. حشو البيانات (Padding)
في حال كانت القطعة الأخيرة من البيانات صغيرة جداً (أقل من الحد الأدنى)، يتم استخدام تقنية تسمى **Padding (الحشو)** [4].
*   **الهدف من الحشو:** حماية البيانات وضمان ثبات هيكل الإطار، ويُشبه ذلك بوضع مواد حشو داخل كرتون زجاج لمنعه من التحرك والكسر أثناء النقل [4, 5].
*   **مكان الإضافة:** يتم إضافة الحشو داخل منطقة **البيانات (Data)** وليس في الهيدر، لأن قيم الهيدر يجب أن تظل ثابتة لضمان صحة العنوان [6].

### 5. الحدود الدنيا والقصوى لحجم الإطار (Frame Size)
وضع نظام الإيثرنت قواعد صارمة لحجم الإطارات المقبولة:
*   **أصغر حجم للإطار (Minimum Frame):** هو **64 بايت** (يتكون من 46 بايت بيانات + الهيدر والفحص) [7].
*   **أكبر حجم للإطار (Maximum Frame):** هو **1518 بايت** (يتكون من 1500 بايت بيانات + 18 بايت للهيدر والفحص) [7].
*   **قاعدة الخطأ:** إذا وصل إطار بحجم **أقل من 64 بايت** أو **أكبر من 1518 بايت**، فإنه يُعتبر إطاراً خاطئاً ويتم التخلص منه (Drop) ويطلب إعادة إرساله [7].

باختصار، تضمن هذه العمليات انتقال البيانات بشكل منظم وآمن، حيث يتم تقطيعها لتناسب سعة الشبكة، وحشوها لضمان الحد الأدنى من الحجم، وفحصها للتأكد من خلوها من الأخطاء بناءً على معايير الحجم المحددة [4, 7].

بناءً على المصادر، إليك الإجابة المفصلة على تساؤلاتك حول تقنيات إطار الإيثرنت:

### لماذا يتم التخلص من الإطارات التي يقل حجمها عن 64 بايت؟
يتم التخلص من هذه الإطارات لأن نظام الإيثرنت لديه **قاعدة محددة لحجم الإطار**؛ حيث أن **أصغر حجم مسموح به هو 64 بايت** وأكبر حجم هو 1518 بايت [1]. إذا وصل إطار بحجم أقل من 64 بايت أو أكبر من 1518 بايت، فإنه يُعتبر **"إطاراً خاطئاً" (Error Frame)** يحتوي على خلل ما، وبالتالي يتم حذفه ويُطلب من المرسل إعادة إرساله [1].

### أين تضاف عملية الحشو (Padding) ولماذا؟
*   **أين تضاف؟** تضاف عملية الحشو **داخل منطقة البيانات (Data)** فقط، ولا يمكن إضافتها في "الهيدر" (Header) لأن ذلك سيؤدي إلى تغيير قيم العناوين المسجلة فيه [2].
*   **لماذا تضاف؟** تُستخدم عندما تكون البيانات في الإطار الأخير غير كافية للوصول إلى الحد الأدنى للحجم المطلوب [4، 5]. والهدف منها هو **حماية البيانات وضمان ثبات الإطار**؛ حيث يشبّه المصدر ذلك بوضع "قش" أو مواد حشو داخل كرتون يحتوي على زجاج؛ فإذا لم يكن الكرتون ممتلئاً، سيتحرك الزجاج وينكسر، لذا يعمل الحشو على تثبيت البيانات ومنع تضررها أثناء النقل [4، 5].

### ما هو مصطلح الـ MTU؟
مصطلح **MTU** هو اختصار لـ **(Maximum Transfer Unit)** أو وحدة النقل القصوى [3]. وهو يشير إلى **أقصى حجم للباكيت (Packet)** يمكن إرساله عبر الشبكة، وعادة ما يكون **1500 بايت** في شبكات الإيثرنت [3، 4]. تبرز أهميته لأن الملفات الكبيرة (مثل ملف بحجم 5 أو 10 ميجا) لا يمكن إرسالها دفعة واحدة، بل يجب تقطيعها إلى أجزاء صغيرة بحجم الـ MTU لتسهيل نقلها عبر الشبكة وإعادة تجميعها عند الطرف الآخر [3، 4].


تناول المهندس في حديثه مجموعة من المفاهيم المتقدمة في هندسة الشبكات، مركزاً على معمارية سيسكو الهرمية ومنطق تلخيص المسارات (Route Summarization). إليك شرح مفصل لما ورد في المصدر:

### أولاً: معمارية سيسكو الهرمية (Cisco Hierarchical Network Design)
تعتمد سيسكو مفهوم "التصميم الهرمي" لضمان إدارة أفضل وسرعة عالية في نقل البيانات، وتتكون من الطبقات التالية التي ذكرها المهندس:

*   **طبقة الـ Core (القلب):** هي الطبقة المسؤولة عن التوصيل السريع جداً وربط المباني ببعضها، حيث تتعامل مع كميات هائلة من البيانات [1].
*   **طبقة الـ Distribution (التوزيع):** هي الطبقة التي يحدث فيها "التوجيه" (Routing) وتطبيق السياسات والأمن، وتعمل كوصلة بين طبقة الوصول والقلب [1، 2].
*   **طبقة الـ Access (الوصول):** هي الطبقة التي يتصل بها المستخدمون (طلاب، موظفون، إلخ)، ويتم فيها تقسيمهم إلى شبكات وهمية (VLANs) لعزل البيانات، ولا يمكنهم التواصل إلا عبر التوجيه في الطبقات الأعلى [2].

### ثانياً: مفهوم "القطر" (Diameter) وقاعدة الـ 6 سويتشات
أوضح المهندس سبباً جوهرياً لاعتماد سيسكو لهذا المخطط، وهو مفهوم **الـ Diameter**:
*   **تعريف الـ Diameter:** هو عدد السويتشات التي تعبر من خلالها حزمة البيانات من المصدر (Source) إلى الوجهة (Destination) [3].
*   **القاعدة:** توصي سيسكو بألا يزيد هذا القطر عن **6 سويتشات** [4].
*   **السبب التقني (STP):** بروتوكول منع الحلقات (Spanning Tree Protocol - STP) يرسل رسائل تسمى (BPDU) كل ثانيتين للتأكد من سلامة الشبكة [6، 8]. إذا زاد عدد السويتشات عن 6، فقد تضيع هذه الرسائل أو تتأخر، مما يؤدي لحدوث **"حلقة" (Loop)** تتسبب في توقف الشبكة بالكامل [7، 8]. لذا، التصميم الهرمي يقلل القطر عبر ربط سويتشات الوصول مباشرة بطبقة التوزيع [5].

### ثالثاً: تلخيص المسارات (Route Summarization)
استخدم المهندس مثال "فرزة السيارات" (تعز - الحديدة) لتبسيط المفهوم، حيث يتم تجميع عدة وجهات فرعية (مثل الجراح، حيس، البرح) تحت مسمى رحلة واحدة رئيسية لتوفير الجهد والوقت [9، 10، 11].

**1. فوائد التلخيص:**
*   **تصغير جدول التوجيه (Routing Table):** بدلاً من أن يحتوي الجدول على 50 شبكة، يظهر كشبكة واحدة فقط، مما يجعل البحث داخل الجدول أسرع بكثير (يشبهه المهندس بسرعة تنفيذ جملة If الشرطية في البرمجة) [14، 16، 18].
*   **تقليل استهلاك الباندويث (Bandwidth):** بروتوكولات التوجيه (مثل RIP) ترسل تحديثات دورية للجداول؛ إرسال جدول صغير يقلل الضغط على خطوط الـ WAN الضعيفة [17، 18].

**2. شروط التلخيص:**
*   أن تكون جميع الشبكات الفرعية على **نفس المسار الفيزيائي** [19، 20].
*   أن تنتمي جميع الشبكات الفرعية إلى **نفس الشبكة الرئيسية (الأم)** [20، 21].

### رابعاً: المسألة وطريقة الحساب (المثال التقني)
لشرح كيفية الحساب يدوياً، طرح المهندس مثالاً يتضمن عدة شبكات (مثل 10.1.0.0/24، 10.1.1.0/24، إلخ) واتبع الخطوات التالية:

1.  **التحويل إلى ثنائي (Binary):** يتم كتابة عناوين الشبكات بالصيغة الثنائية [22، 23].
2.  **مقارنة البتات (Bit Comparison):** نبدأ بالمقارنة من اليسار لمعرفة إلى أي "بت" تتشابه جميع العناوين [6].
3.  **تحديد القناع الجديد (Prefix):** عدد البتات المتشابهة يمثل الـ Prefix الجديد (مثلاً إذا تشابهت في أول 18 بت، يصبح القناع /18) [24، 25].
4.  **تحديد عنوان الملخص:** القيم الثابتة في البتات المتشابهة هي التي تشكل عنوان الشبكة الملخصة (Summary Address) [25، 26].

أشار المهندس في الختام إلى أن البروتوكولات الحديثة تقوم بهذا الحساب تلقائياً، ولكن فهم الحساب اليدوي ضروري جداً للاختبارات والتمارين [7].

بناءً على ما ورد في حديث المهندس، إليك توضيحاً للمخاطر والعلاقات التقنية التي استفسرت عنها:

### 1. مخاطر تجاوز قاعدة الـ 6 سويتشات (Diameter)
تكمن المخاطرة الأساسية في أن بروتوكول **STP (Spanning Tree Protocol)** مصمم ليعمل بكفاءة ضمن "قطر" (عدد سويتشات) محدد.
*   **ضياع رسائل الـ BPDU:** يرسل بروتوكول STP رسائل دورية كل **ثانيتين** للتأكد من سلامة المسارات [1]. إذا زاد عدد السويتشات عن 6، فقد تضيع هذه الرسائل أو تتأخر في الوصول [2].
*   **حدوث حلقات (Loops):** عندما تضيع رسائل STP، يعتقد السويتش أن المسار آمن فيفتح منافذ قد تؤدي لتعارض، مما يسبب "لوب" (Loop) داخل الشبكة [2].
*   **توقف الشبكة بالكامل:** حدوث اللوب يؤدي فوراً إلى **توقف الشبكة بالكامل** عن العمل [3].

### 2. تأثير تلخيص المسارات على الباندويث (WAN Bandwidth)
أوضح المهندس أن خطوط الـ **WAN** (التي تربط بين المدن مثلاً) عادة ما تكون **ضعيفة السرعة** مقارنة بالشبكات المحلية [4].
*   **حجم التحديثات (Updates):** بروتوكولات التوجيه (مثل RIP) ترسل جدول التوجيه كاملاً كل 30 ثانية [4].
*   **استهلاك السعة:** إذا كان الجدول ضخماً (يحتوي مثلاً على 50 مساراً منفصلاً)، فإن حجم البيانات المرسلة سيكون كبيراً، مما يستهلك الباندويث المحدود لخط الـ WAN ويسبب بطئاً شديداً أو تعليقاً في الشبكة [17، 18].
*   **التوفير بالتلخيص:** عند التلخيص، يتم إرسال مسار واحد فقط (الشبكة الأم)، مما يجعل حجم التحديث صغيراً جداً، وبالتالي **يقل استهلاك الباندويث** ويتحسن أداء الشبكة [5].

### 3. العلاقة بين بروتوكول STP وحدوث الحلقات (Loops)
يعمل بروتوكول STP كـ "حارس" للشبكة لمنع البيانات من الدوران في حلقات مفرغة:
*   **الرسائل التحذيرية:** يستخدم STP رسائل تسمى **BPDU** كنبضات قلب للشبكة كل ثانيتين [3].
*   **سقوط البروتوكول:** إذا سقط هذا البروتوكول أو فشل في إيصال رسائله بسبب تعقيد الشبكة أو زيادة "القطر" عن الحد المسموح، تفقد الشبكة قدرتها على اكتشاف المسارات المتكررة [3].
*   **النتيجة:** يؤدي هذا الفشل مباشرة إلى نشوء **حلقة بيانات (Loop)**، وهي حالة تدور فيها الحزم بشكل لا نهائي مما يؤدي لانهيار الخدمة [7، 8].

### نقاط إضافية ذكرها المهندس ولم تُغطَّ سابقاً:
*   **قاعدة الـ 100 متر:** ذكر المهندس أن الكابلات (السويتشات) تضمن نقل البيانات لمسافة **100 متر** فقط كحد أقصى، وما بعد 120 متر لا تضمن الشركات وصول الإشارة، لذا يضطر البعض لإضافة سويتشات إضافية لتقوية الإشارة، وهنا يبرز خطر تجاوز "قاعدة الستة" [5، 7].
*   **الجهد على المعالج (CPU Overload):** عملية البحث في جدول توجيه كبير تشبه تنفيذ جمل شرطية (If statements) برمجية معقدة [6]. هذا البحث يتكرر مع **كل رزمة (Packet)** تمر عبر الراوتر، فإذا كان الملف مقطعاً لـ 100 ألف رزمة، سيقوم الراوتر بـ 100 ألف عملية بحث، مما يرهق المعالج ويسبب تأخيراً (Latency) [7].
*   **العزل باستخدام الـ VLANs:** في طبقة الوصول (Access Layer)، يتم تقسيم المستخدمين (طلاب، موظفين) إلى شبكات وهمية (VLANs) لضمان الأمن، بحيث لا يمكنهم التواصل إلا عبر التوجيه في الطبقات العليا [8].
*   **تاريخ القواعد:** أشار إلى وجود قاعدة قديمة كانت تسمى "قاعدة المكررات الأربعة" (4-Hubs rule) لمنع التصادمات، ولكن مع ظهور السويتشات انتهت مشكلة التصادم، وبقيت مشكلة "القطر" المتعلقة ببروتوكول STP [9].