# شرح شامل لعملية تغليف البيانات (Encapsulation) وتصميم الشبكات

يتناول هذا المستند شرحاً مفصلاً لعملية **تغليف البيانات (Encapsulation)** عبر طبقات نموذج OSI السبع، وكيفية تغير مسمياتها ومكوناتها في كل طبقة حتى تصل إلى الوسط الفيزيائي. كما يغطي تفاصيل دقيقة حول هيكلة إطار الإيثرنت، بروتوكولات النقل TCP و UDP، تصميم الشبكات الهرمي من سيسكو، ومفهوم تلخيص المسارات، بالإضافة إلى أمثلة توضيحية وقصص جانبية وردت في الشرح الأصلي.

---

## 1. عملية تغليف البيانات (Encapsulation) وتجزئتها

في كل طبقة من طبقات النموذج السبع، يضاف غلاف للبيانات. هذه العملية تضمن إضافة المعلومات اللازمة لكل طبقة لضمان وصول البيانات بشكل صحيح.

- في الطبقات الثلاث العليا (السابعة، والسادسة، والخامسة)، تظل البيانات محتفظة بشكلها كملف كامل وتسمى **"داتا" (Data)** لأنها لم تتقطع بعد.

- تبدأ عملية **تقطيع الملف (Fragmentation)** إلى قطع صغيرة في الطبقة الرابعة (**Transport Layer**)، وهنا يبدأ تغير اسم البيانات.

### مسميات البيانات في كل طبقة (PDU)

يُطلق على اسم البيانات في كل طبقة مصطلح **PDU** (اختصار لـ Protocol Data Unit)، وهي كالتالي:

- **الطبقة 7، 6، 5:** تسمى **داتا (Data)**.

- **الطبقة 4 (Transport):** تسمى **سيجمنت (Segment)**، وهي عبارة عن الداتا المقطوعة مضافاً إليها هيدر الطبقة الرابعة.

- **الطبقة 3 (Network):** تسمى **باكيت (Packet)**، وهي عبارة عن "السيجمنت" مضافاً إليها هيدر الطبقة الثالثة.

- **الطبقة 2 (Data Link):** تسمى **فريم (Frame)**، وهي عبارة عن "الباكيت" مضافاً إليها هيدر في البداية وتذييل (Trailer) في النهاية.

- **الطبقة 1 (Physical):** تسمى **بتس (Bits)**، حيث تتحول البيانات إلى واحدات وأصفار.

---

## 2. تفاصيل هيدر الطبقات (Headers)

يختلف محتوى الهيدر (الترويسة) بناءً على الطبقة والبروتوكول المستخدم:

- **هيدر الطبقة الرابعة:** يعتمد على البروتوكول؛ ففي **TCP** يحتوي على رقم منفذ المصدر والهدف، وحجم النافذة (Window size)، والرقم التسلسلي (Sequence). أما في **UDP** فيحتوي فقط على المنافذ والـ Checksum.

- **هيدر الطبقة الثالثة (IP Header):** يحتوي على عنوان IP المرسل (Source) والمستقبل (Destination)، ومدة بقاء الحزمة (TTL)، والإصدار (Version)، والـ Checksum.

- **الطبقة الثانية (الفريدة):** هي الطبقة الوحيدة التي تضيف **هيدر وتذييل (Trailer)**. الهيدر يحتوي على عناوين الماك (MAC addresses)، بينما التذييل يُستخدم للتحقق من الأخطاء.

---

## 3. هيكلة "الفريم" (Frame Structure) في الطبقة الثانية

يتكون الفريم (مثل فريم الإيثرنت) من عدة أجزاء:

### أجزاء الفريم

| الجزء | الوصف التفصيلي |
| --- | --- |
| **الـ Preamble (الديباجة)** | هي بايتات ثابتة (أصفار وواحدات) تشير إلى بداية الفريم لتنبيه المستقبل. تتكون من **8 بايتات** (أو 64 بت) من الأصفار والواحدات المتناوبة (10101010)، لكن البايت الأخير ينتهي بـ **(11)** للإشارة إلى نهاية الديباجة وبداية الفريم. |
| **عناوين الماك (MAC Addresses)** | عنوان الماك للمستقبل أولاً، ثم عنوان الماك للمرسل. طول عنوان MAC يبلغ **48 بت**، وهو ما يعادل **6 بايت**. |
| **النوع (Type)** | يحدد نوع البيانات المحمولة. هذا الحقل ضروري جداً عند عملية **فك التغليف (Decapsulation)**؛ فهو الذي يخبر الطبقة الثانية أي بروتوكول في الطبقة الثالثة يجب أن يستلم "الباكيت" (هل هو IPv4 أم IPv6 أم غيرهما). |
| **البيانات (Packet/Data)** | وهي الباكيت القادمة من الطبقة الأعلى. الحزم القادمة من الطبقة الثالثة يتراوح حجمها من **46 إلى 1500 بايت**. |
| **FCS (Frame Check Sequence)** | للتحقق من سلامة البيانات في التذييل. وهو حقل بطول **4 بايت**. |

### تفاصيل دقيقة عن "الديباجة" (Preamble) وتطور وظيفتها

- **تكوينها التقني:** تتكون من **8 بايتات** (أو 64 بت) من الأصفار والواحدات المتناوبة (10101010)، لكن البايت الأخير ينتهي بـ **(11)** للإشارة إلى نهاية الديباجة وبداية الفريم.

- **تطور وظيفتها:** في كروت الشبكة القديمة (سرعة 10)، كانت تُستخدم **للتزامن (Synchronization)** ليعرف المستقبل متى يبدأ الفريم. أما في السرعات الحديثة (100 و1000 وما فوق)، أصبح التزامن يتم عبر دوائر هاردوير خاصة، وبقيت الديباجة فقط من أجل **التوافق (Compatibility)** مع الأجهزة القديمة.

### آلية فحص الأخطاء (FCS/CRC) بالتفصيل

- **خوارزمية CRC:** يقوم المرسل بتشغيل خوارزمية رياضية على بيانات الهيدر والباكيت لتوليد قيمة رقمية تُخزن في حقل **FCS**.

- **المطابقة عند المستقبل:** عند وصول الفريم، يعيد المستقبل حساب القيمة بنفس الخوارزمية؛ إذا تطابقت القيمتان يكون الفريم سليماً، وإذا اختلفتا (حتى لو في بت واحد فقط) يتم **حذف الفريم فوراً**.

- **اكتشاف الخطأ مقابل تصحيحه:** الطبقة الثانية تكتشف الخطأ وتحذف الفريم فقط، لكنها **لا تعيد إرساله**. المسؤول عن طلب إعادة الإرسال وتصحيح الخطأ هو بروتوكول **TCP** في الطبقة الرابعة، أو قد يُترك الأمر للتطبيق في الطبقة السابعة إذا كان البروتوكول المستخدم هو **UDP**.

---

## 4. العناوين الفيزيائية (MAC) مقابل العناوين المنطقية (IP)

هناك قاعدة مهمة جداً فيما يخص حركة البيانات عبر الشبكة:

- **العنوان المنطقي (IP):** يظل **ثابتاً** من بداية الطريق حتى نهايته (من المصدر إلى الهدف النهائي) ولا يتغير.

- **العنوان الفيزيائي (MAC):** هو الذي **يتغير في كل نقطة (Router)** يمر بها القلم (البيانات). ففي كل راوتر، يتم فك التغليف وتغيير عنوان الماك ليكون المصدر هو الراوتر الحالي والوجهة هي الراوتر التالي، بينما يظل الـ IP ثابتاً.

---

## 5. أحجام البيانات والقيود التقنية

### 1. وحدة النقل القصوى (MTU)

مصطلح **MTU** هو اختصار لـ **(Maximum Transfer Unit)** أو وحدة النقل القصوى. وهو يشير إلى **أقصى حجم للباكيت (Packet)** يمكن إرساله عبر الشبكة، وعادة ما يكون **1500 بايت** في شبكات الإيثرنت. تبرز أهميته لأن الملفات الكبيرة (مثل ملف بحجم 5 أو 10 ميجا) لا يمكن إرسالها دفعة واحدة، بل يجب تقطيعها إلى أجزاء صغيرة بحجم الـ MTU لتسهيل نقلها عبر الشبكة وإعادة تجميعها عند الطرف الآخر.

### 2. حدود حجم الإطار (Frame Size)

وضع نظام الإيثرنت قواعد صارمة لحجم الإطارات المقبولة:

- **أصغر حجم للإطار (Minimum Frame):** هو **64 بايت** (يتكون من 46 بايت بيانات + الهيدر والفحص).

- **أكبر حجم للإطار (Maximum Frame):** هو **1518 بايت** (يتكون من 1500 بايت بيانات + 18 بايت للهيدر والفحص).

- **قاعدة الخطأ:** إذا وصل إطار بحجم **أقل من 64 بايت** أو **أكبر من 1518 بايت**، فإنه يُعتبر **"إطاراً خاطئاً" (Error Frame)** يحتوي على خلل ما، وبالتالي يتم حذفه ويُطلب من المرسل إعادة إرساله.

### 3. حشو البيانات (Padding)

تضاف عملية الحشو **داخل منطقة البيانات (Data)** فقط، ولا يمكن إضافتها في "الهيدر" (Header) لأن ذلك سيؤدي إلى تغيير قيم العناوين المسجلة فيه. تُستخدم عندما تكون البيانات في الإطار الأخير غير كافية للوصول إلى الحد الأدنى للحجم المطلوب. والهدف منها هو **حماية البيانات وضمان ثبات الإطار**؛ حيث يشبّه المصدر ذلك بوضع "قش" أو مواد حشو داخل كرتون يحتوي على زجاج؛ فإذا لم يكن الكرتون ممتلئاً، سيتحرك الزجاج وينكسر، لذا يعمل الحشو على تثبيت البيانات ومنع تضررها أثناء النقل.

---

## 6. بروتوكولات النقل (TCP vs UDP)

### أولاً: ترويسة بروتوكول TCP (Transmission Control Protocol)

تعد ترويسة TCP معقدة لأنها مصممة لضمان موثوقية نقل البيانات، ويبلغ طولها الأدنى **20 بايت** (بدون الخيارات الإضافية). وتتكون من الحقول التالية:

- **منفذ المصدر والوجهة (Source & Destination Ports):** يخصص لكل منهما **16 بت**. عادة ما يكون رقم منفذ المصدر عشوائياً وأكبر من 1023، بينما يحدد منفذ الوجهة نوع الخدمة (مثلاً المنفذ 23 مخصص لخدمة Telnet).

- **الرقم التسلسلي (Sequence Number):** طوله **32 بت**، ويستخدم لترتيب الرزم وضمان وصولها بالتسلسل الصحيح.

- **رقم التأكيد (Acknowledgment Number):** طوله **32 بت**، وهو يمثل **الرقم المتوقع التالي** للرزمة التي وصلت بنجاح. فإذا استلم المستقبل 10000 رزمة، فإنه يرسل رقم التأكيد 10001 ليخبر المرسل أنه ينتظر الرزمة التالية.

- **طول الترويسة (Header Length) والحقل المحجوز (Reserved):** يخصص **4 بت** لطول الترويسة، و **4 بت** أخرى كحقل محجوز للاستخدامات المستقبلية.

- **الأعلام (Flags):** هي مؤشرات تحدد حالة الرزمة، مثل:
  - **SYN:** لبدء الاتصال (Synchronization).
  - **ACK:** للتأكيد على استلام البيانات.
  - **FIN:** لإنهاء الاتصال.
  - **RST:** لإعادة ضبط الاتصال في حال حدوث خطأ.

- **حجم النافذة (Window Size):** يخصص له **16 بت**، ووظيفته **تنسيق سرعة الإرسال** بين المرسل والمستقبل (Flow Control) لضمان عدم إرسال بيانات أسرع مما يمكن للمستقبل معالجته.

- **التحقق من الخطأ (Checksum) والمؤشر العاجل (Urgent Pointer):** يخصص لكل منهما **16 بت**.

### ثانياً: ترويسة بروتوكول UDP (User Datagram Protocol)

يتميز UDP بكونه بروتوكولاً بسيطاً وسريعاً جداً لأن ترويسته صغيرة جداً مقارنة بـ TCP، حيث يبلغ طولها **8 بايت** فقط. وتتكون من:

- **منفذ المصدر (16 بت)**.

- **منفذ الوجهة (16 بت)**.

- **الطول (16 بت)**.

- **حقل التحقق (Checksum) (16 بت)**.

- **ملاحظة:** لا يحتوي UDP على ميزات مثل حجم النافذة، الأعلام، الأرقام التسلسلية، أو خيارات الأمان، مما يجعله أسرع لكنه أقل موثوقية.

### ثالثاً: تقسيم أرقام المنافذ (Port Numbers)

تنقسم أرقام المنافذ إلى ثلاثة أنواع رئيسية:

1. **المنافذ المعروفة (Well-known Ports):** من **0 إلى 1023**، وهي محجوزة لخدمات محددة.

1. **المنافذ المسجلة (Registered Ports):** من **1024 إلى 49151**.

1. **المنافذ الديناميكية أو الخاصة (Dynamic/Private):** من **49152 إلى 65535**، وهي مجانية للاستخدام داخل الشبكات المحلية.

---

## 7. معمارية سيسكو الهرمية (Cisco Hierarchical Network Design)

تعتمد سيسكو مفهوم "التصميم الهرمي" لضمان إدارة أفضل وسرعة عالية في نقل البيانات، وتتكون من الطبقات التالية:

- **طبقة الـ Core (القلب):** هي الطبقة المسؤولة عن التوصيل السريع جداً وربط المباني ببعضها، حيث تتعامل مع كميات هائلة من البيانات.

- **طبقة الـ Distribution (التوزيع):** هي الطبقة التي يحدث فيها "التوجيه" (Routing) وتطبيق السياسات والأمن، وتعمل كوصلة بين طبقة الوصول والقلب.

- **طبقة الـ Access (الوصول):** هي الطبقة التي يتصل بها المستخدمون (طلاب، موظفون، إلخ)، ويتم فيها تقسيمهم إلى شبكات وهمية (VLANs) لعزل البيانات، ولا يمكنهم التواصل إلا عبر التوجيه في الطبقات الأعلى.

### مفهوم "القطر" (Diameter) وقاعدة الـ 6 سويتشات

أوضح المهندس سبباً جوهرياً لاعتماد سيسكو لهذا المخطط، وهو مفهوم **الـ Diameter**:

- **تعريف الـ Diameter:** هو عدد السويتشات التي تعبر من خلالها حزمة البيانات من المصدر (Source) إلى الوجهة (Destination).

- **القاعدة:** توصي سيسكو بألا يزيد هذا القطر عن **6 سويتشات**.

- **السبب التقني (STP):** بروتوكول منع الحلقات (Spanning Tree Protocol - STP) يرسل رسائل تسمى (BPDU) كل ثانيتين للتأكد من سلامة الشبكة. إذا زاد عدد السويتشات عن 6، فقد تضيع هذه الرسائل أو تتأخر، مما يؤدي لحدوث **"حلقة" (Loop)** تتسبب في توقف الشبكة بالكامل. لذا، التصميم الهرمي يقلل القطر عبر ربط سويتشات الوصول مباشرة بطبقة التوزيع.

---

## 8. تلخيص المسارات (Route Summarization)

استخدم المهندس مثال "فرزة السيارات" (تعز - الحديدة) لتبسيط المفهوم، حيث يتم تجميع عدة وجهات فرعية (مثل الجراح، حيس، البرح) تحت مسمى رحلة واحدة رئيسية لتوفير الجهد والوقت.

### 1. فوائد التلخيص:

- **تصغير جدول التوجيه (Routing Table):** بدلاً من أن يحتوي الجدول على 50 شبكة، يظهر كشبكة واحدة فقط، مما يجعل البحث داخل الجدول أسرع بكثير (يشبهه المهندس بسرعة تنفيذ جملة If الشرطية في البرمجة).

- **تقليل استهلاك الباندويث (Bandwidth):** بروتوكولات التوجيه (مثل RIP) ترسل تحديثات دورية للجداول؛ إرسال جدول صغير يقلل الضغط على خطوط الـ WAN الضعيفة.

### 2. شروط التلخيص:

- أن تكون جميع الشبكات الفرعية على **نفس المسار الفيزيائي**.

- أن تنتمي جميع الشبكات الفرعية **لنفس الشبكة الكبرى**.

### 3. المسألة وطريقة الحساب (المثال التقني)

لشرح كيفية الحساب يدوياً، طرح المهندس مثالاً يتضمن عدة شبكات (مثل 10.1.0.0/24، 10.1.1.0/24، إلخ) واتبع الخطوات التالية:

1. **التحويل إلى ثنائي (Binary):** يتم كتابة عناوين الشبكات بالصيغة الثنائية.

1. **مقارنة البتات (Bit Comparison):** نبدأ بالمقارنة من اليسار لمعرفة إلى أي "بت" تتشابه جميع العناوين.

1. **تحديد القناع الجديد (Prefix):** عدد البتات المتشابهة يمثل الـ Prefix الجديد (مثلاً إذا تشابهت في أول 18 بت، يصبح القناع /18).

1. **تحديد عنوان الملخص:** القيم الثابتة في البتات المتشابهة هي التي تشكل عنوان الشبكة الملخصة (Summary Address).

أشار المهندس في الختام إلى أن البروتوكولات الحديثة تقوم بهذا الحساب تلقائياً، ولكن فهم الحساب اليدوي ضروري جداً للاختبارات والتمارين.

---

## 9. أمثلة وقصص توضيحية من المحاضر

تضمن الشرح الأصلي أمثلة وقصصاً جانبية لتبسيط المفاهيم:

- **قصة الـ 50 ألف ريال:** ضرب المحاضر مثالاً بأب يرسل لابنه 50 ألف ريال مع "صاحب صالون" ويرسل معه رسالة ورقية توضح المبلغ؛ فإذا وجد الابن المبلغ ناقصاً عن المكتوب في الرسالة، سيعرف أن هناك خطأ (وهذا يشبه دور الـ FCS في كشف التلاعب بالبيانات).

- **الخلفية التاريخية للتقنية:** ذكر المحاضر ذكرياته عن دراسة الكمبيوتر في نهاية التسعينات (تخرج عام 99)، وتحدث عن مواصفات الأجهزة القديمة مثل معالجات Pentium 1 ورامات بسعة ضئيلة جداً، وكيف كانت سرعات الشبكة بطيئة وتحتاج "للتزامن" اليدوي.

- **التطبيقات العملية للهاش (Hash):** أشار إلى أن فكرة الـ CRC تشبه الـ Hash المستخدم عند تحميل أنظمة التشغيل (مثل IOS للراوترات) للتأكد من أن الملف سليم ولم يتم التلاعب به من قبل مخترقين.

---

**ملاحظة:** تم إعداد هذا المستند لضمان شمولية المعلومات ودقتها التقنية، مع تضمين كافة التفاصيل والأمثلة التوضيحية التي وردت في النص الأصلي.