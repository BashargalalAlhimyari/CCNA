# أساسيات الـ Subnetting

* **المشكلة:** وجود شبكة واحدة كبيرة (مثل `192.168.10.0/24`) يعني وجود **Broadcast Domain** واحد كبير، وهذا يسبب زيادة في حركة الـ Broadcast ويقلل كفاءة الشبكة.
* **الحل:** تقسيم الشبكة الكبيرة إلى عدة شبكات أصغر باستخدام **Subnetting**.
* مثال:

  * قبل: شبكة واحدة `192.168.10.0/24`.
  * بعد: عدة شبكات مثل:

    * `192.168.10.0`
    * `192.168.10.32`
    * `192.168.10.64`
    * `192.168.10.96`
* الفائدة:

  * تقليل حجم الـ Broadcast Domain.
  * تنظيم الشبكة بشكل أفضل.
  * تسهيل الإدارة وتحسين الأداء.

---

# كيف يتم إنشاء Subnets؟

الفكرة الأساسية:

* نأخذ (**Borrow**) بعض البتات من جزء **Host** ونستخدمها لإنشاء **Subnets** جديدة.
* كلما زاد عدد البتات المستعارة:

  * يزيد عدد الـ **Subnets**.
  * يقل عدد الـ **Hosts** في كل Subnet.

**مهم:** هناك دائمًا موازنة بين عدد الشبكات وعدد الأجهزة داخل كل شبكة.

---

# قبل البدء في عمل Subnetting

يجب تحديد احتياجات الشبكة أولًا:

### 1. تحديد عدد الشبكات المطلوبة (Network IDs)

احسب:

* Subnet لكل LAN.
* Subnet لكل اتصال WAN بين الراوترات.

---

### 2. تحديد عدد الأجهزة المطلوبة في كل Subnet (Host IDs)

احسب:

* عنوان IP لكل جهاز (TCP/IP Host).
* عنوان IP لكل منفذ Router Interface.

---

### 3. بعد معرفة المتطلبات قم بتحديد:

* Subnet Mask مناسب للشبكة.
* Subnet ID مختلف لكل شبكة.
* مدى عناوين الـ Hosts لكل Subnet.

---

# Classful Routing

في هذا الجزء من الكتاب يتم شرح **Classful Routing**.

معناه:

* جميع الشبكات تستخدم **نفس Subnet Mask**.

لاحقًا ستتعلم **VLSM (Variable Length Subnet Mask)** وهو:

* يسمح لكل شبكة باستخدام **Subnet Mask مختلف** حسب الحاجة.
* يسمى **Classless Routing**.

---

# Subnet Mask

* الـ **Subnet Mask** هو قيمة مكونة من **32 بت**.
* وظيفته تحديد:

  * أي جزء من عنوان IP يمثل **Network/Subnet**.
  * وأي جزء يمثل **Host**.
* البتات التي قيمتها **1** تمثل جزء **Network/Subnet**.
* البتات التي قيمتها **0** تمثل جزء **Host**.

---

# Default Subnet Masks

| الفئة   | Default Mask    |
| ------- | --------------- |
| Class A | `255.0.0.0`     |
| Class B | `255.255.0.0`   |
| Class C | `255.255.255.0` |

---

# ملاحظات مهمة

* إذا لم تستخدم Subnetting، فهذا يعني أنك تستخدم **Default Subnet Mask**.
* لا يُنصح بتغيير الـ Default Mask بطريقة لا تناسب فئة العنوان (Class)، خاصة في دراسة CCNA.
* الحد الأدنى:

  * Class A يبدأ بـ `255.0.0.0`.
  * Class B يبدأ بـ `255.255.0.0`.
  * Class C يبدأ بـ `255.255.255.0`.
* القناع `255.255.255.255` (كلها 1) **ليس Subnet Mask عاديًا للشبكات**، بل يمثل عنوانًا خاصًا (Single Host/All bits set)، لذلك لا يُستخدم كقناع لشبكة في هذا السياق.

**الخلاصة:** فكرة الـ **Subnetting** هي استعارة بتات من جزء الـ **Host** لإنشاء شبكات أصغر وأكثر تنظيمًا، مع تقليل عدد الأجهزة الممكنة داخل كل شبكة مقابل زيادة عدد الشبكات.


# Classless Inter-Domain Routing (CIDR)

* **CIDR** هو اختصار لـ **Classless Inter-Domain Routing**.
* يستخدمه **مزودو خدمة الإنترنت (ISP)** لتوزيع عناوين IP على العملاء.
* يتم إعطاء العميل **مجموعة (Block)** من عناوين IP بدلًا من عنوان واحد.

مثال:

`192.168.10.32/28`

هذا يعني:

* عنوان الشبكة هو `192.168.10.32`.
* الرقم بعد الشرطة المائلة (`/28`) يحدد **Subnet Mask**.

---

# ما معنى Slash Notation؟

الرقم بعد `/` يمثل:

> **عدد البتات التي قيمتها 1 في الـ Subnet Mask.**

أمثلة:

| Slash | Subnet Mask     |
| ----- | --------------- |
| /8    | 255.0.0.0       |
| /16   | 255.255.0.0     |
| /24   | 255.255.255.0   |
| /30   | 255.255.255.252 |

كلما زاد الرقم بعد `/`:

* يزيد عدد بتات الـ Network.
* يقل عدد بتات الـ Host.

---

# لماذا الحد الأقصى هو /32؟

عنوان IPv4 يتكون من:

* 4 Bytes.
* كل Byte = 8 Bits.

إذًا:

**4 × 8 = 32 Bit**

لذلك أكبر قيمة ممكنة نظريًا هي:

`/32`

لكن في **CCNA** عند دراسة الشبكات التي تحتوي Hosts، أكبر Subnet Mask ستستخدمها هو:

`/30`

**السبب:**

* يجب أن يبقى **بتان (2 Bits)** على الأقل للـ Hosts.
* لأن الشبكة تحتاج إلى:

  * عنوان Network.
  * عنوان Broadcast.
  * وعناوين للأجهزة.

---

# أمثلة

### Class A

Default Mask:

`255.0.0.0`

بالثنائي:

`11111111.00000000.00000000.00000000`

عدد الـ 1 = **8**

إذن:

`255.0.0.0 = /8`

---

### Class B

Default Mask:

`255.255.0.0`

بالثنائي:

`11111111.11111111.00000000.00000000`

عدد الـ 1 = **16**

إذن:

`255.255.0.0 = /16`

---

# أشهر قيم CIDR

| CIDR | Subnet Mask     |
| ---- | --------------- |
| /8   | 255.0.0.0       |
| /16  | 255.255.0.0     |
| /24  | 255.255.255.0   |
| /25  | 255.255.255.128 |
| /26  | 255.255.255.192 |
| /27  | 255.255.255.224 |
| /28  | 255.255.255.240 |
| /29  | 255.255.255.248 |
| /30  | 255.255.255.252 |

---

# ملاحظات مهمة

* **CIDR** هو مجرد **طريقة مختصرة لكتابة Subnet Mask** باستخدام `/`.
* لحساب قيمة CIDR، **عدّ عدد البتات (1) في الـ Subnet Mask**.
* احفظ التحويلات الأساسية لأنها كثيرة الاستخدام في CCNA، خاصة:

  * `/8`
  * `/16`
  * `/24`
  * `/25`
  * `/26`
  * `/27`
  * `/28`
  * `/29`
  * `/30`
* الأقنعة من **`/8` إلى `/15`** تُستخدم مع **Class A**.
* الأقنعة من **`/16` إلى `/23`** تُستخدم مع **Class B** (وسيُكمل الكتاب باقي النطاقات في الصفحة التالية).

